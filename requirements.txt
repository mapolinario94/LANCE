# Core dependencies for LANCE fine-tuning experiments.
# Tested with the versions noted in comments; newer minor versions should work.
torch>=2.0          # tested: 2.12.0
torchvision>=0.15   # tested: 0.27.0
timm>=1.0           # tested: 1.0.27  (required for main_deit.py / DeiT-Tiny)
wandb>=0.16         # tested: 0.28.0  (experiment logging)
numpy>=1.23         # tested: 2.4.4

# mcunet is NOT on PyPI in the version we use. It is only required for
# `main.py --arch mcunet`. Install it from source (see README.md "MCUNet").
